import styled from 'styled-components';
import { COLORS } from '@src/constants';

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  padding: 16px;
  width: 100%;
  background: ${COLORS.darkGray130};
  color: ${COLORS.white};
`;
